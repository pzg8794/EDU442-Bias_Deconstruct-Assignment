# Overleaf-linked Repo

The companion repo for the writing workflow is:

- `pzg8794/EDU442-Bias_Deconstruct-Assignment`

## Intended flow
- `draft1/` = current working draft for review
- `draft2/` = next revision pass after review

## Note on submodule status
A true git submodule entry was not created through this connector workflow. This file serves as the clean repo pointer so the two workspaces stay aligned without risking repo corruption.
